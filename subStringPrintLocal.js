/**
 * @param {number[][]} matrix
 * @return {number}
 */
var subStringPrintLocal = function (str) {
    let sub = []
    for (let i = 0; i < str.length; i++) {
        let rem = str.slice(i + 1, str.length)
        let x = 1

    }
};

let str = "abcd"
console.log(subStringPrintLocal(str))